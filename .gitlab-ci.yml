image: node:6.12.3-alpine

variables:
  GIT_STRATEGY: clone
  GIT_CHECKOUT: "false"

before_script:
  - apk update && apk upgrade

install_project:
  script:
    - git show
    - yarn
    - yarn lerna updated
    - yarn test:libraries

